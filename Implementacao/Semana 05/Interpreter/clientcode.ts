import { FoodInterpreter } from "./FoodInterpreter/FoodInterpreter";

const interpreter = new FoodInterpreter();

console.log(interpreter.interpret("french fries")); 
console.log(interpreter.interpret("hot dog")); 
console.log(interpreter.interpret("ice cream")); 
